<% if object.errors.any? %>
  <div>
    <div>
       有 <%= pluralize(object.errors.count,"个错误") %>
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
      <li class="errors_li"><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>