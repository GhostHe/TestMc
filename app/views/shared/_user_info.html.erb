<%= link_to gravatar_for(current_user, size: 60),current_user %>
<%= current_user.name %>
<span><%= link_to "我的页面",current_user %></span>
<span><%= pluralize(current_user.microposts.count,"micropost") %></span>